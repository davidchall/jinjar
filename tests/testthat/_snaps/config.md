# input validation works

    Conflicting delimiters: variable_open, block_open

# default works

    Code
      print(x)
    Output
      Loader: disabled
      Syntax: {% block %} {{ variable }} {# comment #}

